export interface UserNameInterface {
  userName: string;
  setUserName: React.Dispatch<React.SetStateAction<UserNameInterface>>;
}

export interface UserPasswordInterface {
  password: string;
}
export interface UserPromise {
  response: {};
}
